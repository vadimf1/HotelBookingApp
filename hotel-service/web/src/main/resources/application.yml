spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel_db?stringtype=unspecified
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false